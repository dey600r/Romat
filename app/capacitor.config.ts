import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'deyapps.ionic.romat',
  appName: 'Romat',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
